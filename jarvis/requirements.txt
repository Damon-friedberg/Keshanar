# Core agent
claude-agent-sdk>=0.1.0
anthropic>=0.40.0
python-dotenv>=1.0.0

# Voice (local STT + mic)
faster-whisper>=1.0.0
sounddevice>=0.4.6
numpy>=1.26

# TTS (ElevenLabs cloud; pyttsx3 is the offline fallback)
elevenlabs>=1.0.0
pyttsx3>=2.90

# Orb HUD bridge (optional)
websockets>=12.0

# Screen capture (talk + show -> tasks)
mss>=9.0
Pillow>=10.0
